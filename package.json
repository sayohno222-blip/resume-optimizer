{
  "name": "resume-optimizer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd client && npx vite",
    "build": "cd client && npx vite build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
